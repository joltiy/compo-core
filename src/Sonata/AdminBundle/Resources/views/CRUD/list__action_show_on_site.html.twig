{% if object is defined %}
    <a class="btn btn-sm btn-default"
       href="{{ admin.routeGenerator.generate(actions.route, { id: object.id} ) }}"
       target="_blank"
    >
        <i class="fa fa-eye"></i>
    </a>
{% endif %}