sonata_page:
    templates:

        default:
            containers:
                 header:
                     blocks:
                        - compo_feedback.block.service.feedback_form
                 content_top:
                     blocks:
                        - compo_feedback.block.service.feedback_form
                 content:
                     blocks:
                        - compo_feedback.block.service.feedback_form
                 content_bottom:
                     blocks:
                        - compo_feedback.block.service.feedback_form