sonata_page:
    templates:
        default:
            containers:
                 header:
                     blocks:
                        - compo_menu.block.service.menu

                 content_top:
                     blocks:
                        - compo_menu.block.service.menu

                 content:
                     blocks:
                        - compo_menu.block.service.menu

                 content_bottom:
                     blocks:
                        - compo_menu.block.service.menu
                 footer:
                     blocks:
                        - compo_menu.block.service.menu

